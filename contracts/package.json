{
  "name": "tele-basket-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "typechain": "hardhat typechain",
    "deploy": "hardhat --network hardhat deploy",
    "deploy:local": "hardhat --network hardhat run scripts/deploy.ts",
    "deploy:paseo": "hardhat --network paseo run scripts/deploy.ts",
    "redeploy:paseo": "hardhat --network paseo run scripts/redeploy.ts",
    "deploy:westend": "hardhat --network westend run scripts/deploy.ts",
    "check:balance": "hardhat run scripts/check-balance.ts",
    "check:basket": "hardhat --network paseo run scripts/check-basket.ts",
    "check:xcm": "hardhat --network paseo run scripts/check-xcm.ts",
    "health:paseo": "hardhat --network paseo run scripts/health-paseo.ts",
    "simulate:deposit": "hardhat --network paseo run scripts/simulate-deposit.ts",
    "disable:xcm": "hardhat --network paseo run scripts/disable-xcm.ts",
    "fund:wallet": "hardhat run scripts/fund-wallet.ts",
    "fund:sovereign": "hardhat --network paseo run scripts/fund-sovereign.ts",
    "deploy:xcm-precompile": "hardhat --network paseo run scripts/deploy-xcm-precompile.ts",
    "verify:sovereign": "hardhat --network paseo run scripts/verify-sovereign.ts",
    "verify:xcm": "hardhat --network paseo run scripts/verify-xcm.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^3.0.4",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/chai": "^5.2.3",
    "chai": "^6.2.2",
    "dotenv": "^17.3.1",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.11.0"
  }
}
