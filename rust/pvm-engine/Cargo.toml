[package]
name = "polkabasket-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
fixed = { version = "1.23", default-features = false }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
